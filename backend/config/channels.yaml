# YouTube channels the morning brief should check for new uploads.
# Use the channel handle (with the leading @) or the raw channel ID (UCxxxx...).
channels:
  - "@JantaKaReporter"
  - "@buffalobills"
  - "@bayyinah"
  - "@BillsNews"
  - "@FRANCE24"
  - "@Firstpost"
  - "@breakingpoints"
